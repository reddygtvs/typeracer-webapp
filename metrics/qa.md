# Validation scope

- Production HTTPS and Cloudflare asset headers.
- Home-page TypeRacer link and production build.
- Sample data, CSV upload, invalid CSV, reset, restore after reload.
- All 20 charts, chart values, percent labels and mobile layout.
- Independent data tests, large input, rolling values, previous-result grouping.
- HTTP time to first byte, full transfer time, bytes, chart request count, data processing time.
- Dependency audit and TypeScript production build.

Browser speed checks include tool overhead. HTTP checks use this Mac and network. They are not global measurements or Core Web Vitals.

Final checks: build, lint and nine automated tests passed. All 20 charts rendered through normal browser scrolling/clicks. Valid upload, invalid accuracy, recovery, sample, reset and restore passed. Desktop/mobile accuracy labels and page width passed. No final production console error was observed. Historical local errors were from intentionally invalid input and the gzip defect fixed during development.
